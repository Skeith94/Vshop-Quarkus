package it.skeith.service;

import javax.enterprise.context.ApplicationScoped;

@ApplicationScoped
public class PhotoService {

/*
    @Value("${postImage.size}")
    private int size;

    @Value("${postImage.extensions}")
    private String[] extensions;


    public boolean checkSize(MultipartFile file){
        if(file.isEmpty() && file.getSize() <= size){
            return true;
        }
        return false;
    }

 */




}
